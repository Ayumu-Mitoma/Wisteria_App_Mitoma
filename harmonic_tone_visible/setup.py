from setuptools import setup, find_packages

setup(
    name="audio_processing",
    version = "0.0.1",
    description = "audio process function for harmonic tone visible app in my app",
    author = "Ayumu Mitoma",
    packages = find_packages(),
    license = "MIT"
)